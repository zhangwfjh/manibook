{
  "$schema": "../node_modules/@tauri-apps/api/config/schema.json",
  "identifier": "main",
  "description": "Main application capabilities",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:default",
    "fs:default"
  ]
}