/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // commented out for gradual Tauri migration
  // trailingSlash: true,
  // images: { unoptimized: true },
};

module.exports = nextConfig;